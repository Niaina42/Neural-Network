import QueryStatsIcon from '@mui/icons-material/QueryStats';

const Tier = ({ color }) => {
  return (
    <QueryStatsIcon sx={{ color: color, height: 30, width: 30 }} />
  );
};

export default Tier;
