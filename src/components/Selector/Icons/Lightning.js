import FunctionsIcon from "@mui/icons-material/Functions";

const Lightning = ({ color }) => {
  return (
    <FunctionsIcon sx={{ color: color, height: 30, width: 30 }} />
  );
};

export default Lightning;
