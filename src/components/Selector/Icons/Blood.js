import PsychologyIcon from '@mui/icons-material/Psychology';

const Blood = ({ color }) => {
  return (
    <PsychologyIcon sx={{ color: color, height: 30, width: 30 }} />
  );
};

export default Blood;